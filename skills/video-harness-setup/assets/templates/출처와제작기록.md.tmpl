# 출처와 제작 기록

외부 자료(스톡 영상·이미지·음악·AI 생성물 등)를 이 폴더에 넣을 때는 아래를 기록한다.

- 출처(URL 또는 제공처)
- 라이선스 종류와 확인한 날짜
- 이 자료를 쓴 회차 또는 위치
- 만든 사람 표기가 필요한지 여부

같은 폴더의 `provenance.json`에 같은 내용을 기계가 읽을 수 있는 형태로 함께 남긴다. 라이선스를 확인하지 못한 자료는 쓰지 않는다.

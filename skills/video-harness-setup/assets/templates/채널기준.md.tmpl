# 채널기준 — {{channel_name}}

- 출발점: {{workflow}}
- 포맷: {{format}}
- 유형: {{kind}}
- 목표 길이: {{target_seconds}}초
- 언어: {{language}}
- 스타일 시작점: {{style_start}}
- 일관성 수준: {{consistency}}
- 대본 운율: {{script_rhythm_label}} (이 채널만 다르게 하려면 여기에 적는다)

## 컨셉

{{concept}}

## 방향

- 시청자: {{audience}}
- 다루는 범위: {{scope}}
- 이 분야와의 관계: {{expertise}}
- 느낌·말투: {{tone}}
- 올리는 곳·주기: {{platforms}}

분야와 한 줄 콘셉트는 위 "컨셉"에 있다. 회차를 시작하기 전에 이 절을 먼저 읽는다.

## 근거 규칙

{{evidence_rules}}

## 오프닝

{{opening}}

## 작업 순서

{{workflow_steps}}

이 순서는 이 채널의 출발점에서 나온 것이다. 출발점을 바꾸면(`harness.config.json`의 `channels.<번호>.workflow`) 이 절도 함께 고친다 — 세팅 스크립트는 이미 만들어진 이 파일을 덮어쓰지 않는다.

## 동의

실존 인물의 얼굴·음성을 쓰는 경우 본인 동의가 필요하다. 동의 없이는 쓰지 않는다.

## 상속하지 않는 것

채널 간 스타일·목소리를 자동 상속하지 않는다. 이 채널의 스타일 값(색, 폰트, 자막 위치, 모션)은 `03_편집프로젝트/_채널공용/frame.md`에서 관리하고, 여기 적은 문장이 그 값과 어긋나면 `frame.md`를 따른다.
